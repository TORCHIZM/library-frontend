import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const MyBooksScreen = () => {
  return (
    <SafeAreaView>
      <Text>Benim Kitaplarım!</Text>
    </SafeAreaView>
  );
};

export default MyBooksScreen;
